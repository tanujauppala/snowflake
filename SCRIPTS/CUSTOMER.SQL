-- Create table
CREATE OR REPLACE TABLE MARKETING_DB.PUBLIC.CLIENT (
    CLIENT_NUMBER INT,
    CLIENT_NAME STRING,
    EMAIL_ADDRESS STRING
);

-- Insert sample data
INSERT INTO MARKETING_DB.PUBLIC.CLIENT (CLIENT_NUMBER, CLIENT_NAME, EMAIL_ADDRESS)
VALUES (1,'Alice','alice@email.com'), 
       (2,'Bob','bob@email.com');

-- Select to verify
SELECT * FROM MARKETING_DB.PUBLIC.CLIENT;
