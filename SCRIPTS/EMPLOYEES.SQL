-- Create table
CREATE OR REPLACE TABLE HR_DB.PUBLIC.EMPLOYEES (
    EMPLOYEE_ID INT,
    EMPLOYEE_NAME STRING,
    DEPARTMENT STRING
);

-- Insert sample data
INSERT INTO HR_DB.PUBLIC.EMPLOYEES (EMPLOYEE_ID, EMPLOYEE_NAME, DEPARTMENT)
VALUES (1,'John','IT'), 
       (2,'Mary','HR');

-- Select to verify
SELECT * FROM HR_DB.PUBLIC.EMPLOYEES;
