-- Create table
CREATE OR REPLACE TABLE FIN_DB.REPORTS.TXN (
    TRANSACTION_ID INT,
    ACCT_ID INT,
    TXN_AMOUNT NUMBER
);

-- Insert sample data
INSERT INTO FIN_DB.REPORTS.TXN (TRANSACTION_ID, ACCT_ID, TXN_AMOUNT)
VALUES (101, 1, 250), 
       (102, 2, 400);

-- Select to verify
SELECT * FROM FIN_DB.REPORTS.TXN;
